import 'package:flutter/material.dart';

class AppColors {
  // ==========================================
  // WARNA ORIGINAL (Dipakai di Customer & Auth)
  // Jangan dihapus agar kode sebelumnya tidak error
  // ==========================================
  static const Color primaryOrange = Color(0xFFF77F00);
  static const Color white = Colors.white;
  static const Color textDark = Color(0xFF2B2D42);

 
  static const Color fireRed = Color(0xFFE63946);      
  static const Color fireOrange = Color(0xFFFF7A00);   
  
  // Warna Netral Tambahan
  static const Color textLight = Color(0xFF8D99AE);    
  static const Color background = Color(0xFFF8F9FA);   
}